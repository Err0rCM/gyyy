#include <stdio.h>
#include <string.h>
int main()
{
	int tail,i=0,j;
	char s[10][10];
	scanf("tail %d",&tail);getchar();
	gets(s[i]);
	while(strcmp(s[i],"")!=0)
		gets(s[++i]);
	for(j=i+tail;j<i;j++)
		puts(s[j]);
	return 0;
}
